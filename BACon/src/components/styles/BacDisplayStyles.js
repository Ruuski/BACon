import { StyleSheet } from 'react-native';

const BacDisplayStyles = StyleSheet.create({
  container: {
    padding: 20,
    justifyContent: 'flex-start',
    alignItems: 'center'
  },

  title: {
    fontSize: 26,
    color: 'black'
  }
})

export default BacDisplayStyles;
