import { StyleSheet } from 'react-native';

const ConfigBarStyles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    flexDirection: 'column',
    alignItems: 'center',
  },
  settingsText: {
    textAlign: 'center',
    fontSize: 18,
  }
})

export default ConfigBarStyles;
