import { StyleSheet } from 'react-native';

const DrinkHistoryStyles = StyleSheet.create({
  container: {
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
  },
  title: {
    fontSize: 30,
    fontFamily: 'Roboto'
  }
})

export default DrinkHistoryStyles;
